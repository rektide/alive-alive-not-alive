{
	"name": "alive-alive-not-alive",
	"version": "1.0.0",
	"description": "Filter a list of hostnames (provided on stdin) to alive/pingable (stdout) or not (stderr).",
	"main": "alive-alive-not-alive.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"bins": {
		"alive-alive-not-alive": "./alive-alive-not-alive.js"
	},
	"repository": {
		"type": "git",
		"url": "https://archive.eldergods.com/alive-alive-not-alive"
	},
	"keywords": [
		"ping",
		"healthcheck",
		"alive",
		"host"
	],
	"author": "rektide de la faye",
	"license": "X11",
	"dependencies": {
		"byline": "^5.0.0",
		"ping": "^0.2.1"
	}
}
